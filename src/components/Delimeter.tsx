import React from 'react';

const Delimeter = () => {
    return (
        <div className={'delimeter'}>
            <div className={'delimeter__line'}/>
            <span className={'delimeter__text'}>Or</span>
            <div className={'delimeter__line'}/>
        </div>
    );
};

export default Delimeter;